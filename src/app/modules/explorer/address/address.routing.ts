import { Route } from '@angular/router';
import { AddressComponent } from 'app/modules/explorer/address/address.component';

export const addressRoutes: Route[] = [
    {
        path     : '',
        component: AddressComponent

    }
];
